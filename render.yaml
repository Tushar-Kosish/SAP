services:
  - type: web
    name: smartevac-ai
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: PORT
        value: 5000
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        value: free-tier-smartevac-secret-key
